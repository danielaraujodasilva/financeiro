<?php require __DIR__ . '/bootstrap.php'; $instanceId=(int)($_GET['instance_id']??0); if(!$instanceId) exit('Instância obrigatória.'); $auth->requireInstanceAccess($instanceId); $instance=$auth->instanceById($instanceId); $centers=$financial->centers($instanceId); ?><!doctype html><html lang="pt-br"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Centros</title><link rel="stylesheet" href="<?= e(base_path('assets/ui.css')) ?>"></head><body><div class="wrap"><?php financial_nav($instanceId,'centers'); ?><div class="card"><h1 class="headline">Centros financeiros</h1><div class="list"><?php foreach($centers as $c): ?><div class="member"><div class="meta"><strong><?= e($c['name']) ?></strong><span class="muted"><?= e($c['type']) ?></span></div><span class="tag"><?= $c['active']?'ativo':'inativo' ?></span></div><?php endforeach; ?></div></div></div></body></html>
